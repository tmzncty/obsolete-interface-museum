name: Validate exhibit layout

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  exhibit-layout:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Test exhibit-layout validation
        run: python -B -m unittest discover -s tests -p "test_exhibit_layout.py" -v

      - name: Validate checked-in exhibit layout
        run: python -B scripts/validate_exhibit_layout.py
